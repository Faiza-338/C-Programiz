
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,add;

    printf("Enter two number:\n");
    scanf("%d %d",&a,&b);

    add=a+b;
    printf("%d + %d = %d",a,b,add);

    return 0;
}
